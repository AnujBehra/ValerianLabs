import { Link } from 'react-router-dom';
import Reveal from './Reveal';
import SectionIntro from './SectionIntro';
import { trackEvent } from '../utils/analytics';

const solutions = [['01', '↗', 'Custom CRM', 'Manage leads, customers, sales pipelines, follow-ups and customer activity in one place — shaped around how your team sells.'], ['02', '▦', 'Inventory Management', 'Track products, stock movements, warehouses, purchases, sales and low-stock alerts without the spreadsheet maze.'], ['03', '⌘', 'ERP & Business Management', 'Connect purchasing, inventory, sales, employees, operations and reporting into one clear platform.'], ['04', '⌘', 'Internal Tools', 'Replace spreadsheets and repetitive manual processes with focused software made specifically for your team.'], ['05', '◒', 'Business Dashboards', 'Turn your business data into clear, real-time dashboards and reports that help people make better decisions.'], ['06', '⌁', 'Workflow Automation', 'Automate repetitive processes, notifications, approvals and internal workflows so work moves forward.']];
export function ProblemSection() { return <section className="problem section section-tint"><Reveal><div className="container narrow-intro"><p className="eyebrow">The cost of disconnected work</p><h2>Your business shouldn't have to work <em>around</em> your software.</h2><p>When your tools don't fit the way your team operates, small workarounds become expensive problems.</p></div></Reveal><div className="container problem-layout">{[['01', 'Important information scattered across Excel files'], ['02', 'Orders arriving through WhatsApp and phone calls'], ['03', "Employees using different systems that don't talk"], ['04', 'Inventory becoming difficult to track'], ['05', 'Existing software packed with features you don’t need'], ['06', 'Manual reporting taking hours every week'], ['07', 'Departments working in disconnected systems']].map(([number, text]) => <div className="problem-list" key={number}><div><span>{number}</span><p>{text}</p></div></div>)}<div className="problem-answer"><strong>We build software around your workflow instead.</strong><span>→</span></div></div></section>; }
export function SolutionsSection({ preview = false }) { const items = preview ? solutions.slice(0, 6) : solutions; return <section id="solutions" className="solutions section"><div className="container"><SectionIntro eyebrow="Built for your business" title="What can we build <em>for you?</em>">Every business has its own rules, handoffs and goals. Your software should reflect that.</SectionIntro><div className="solution-grid">{items.map(([number, icon, title, description], index) => <Reveal key={title} className={index % 2 ? 'reveal-delay' : ''}><article className={`solution-card ${index === 0 ? 'featured' : ''}`}><span className="card-number">{number}</span><div className="solution-icon">{icon}</div><h3>{title}</h3><p>{description}</p><Link to="/contact">Explore this solution <span>↗</span></Link></article></Reveal>)}</div></div></section>; }
export function ProcessSection() { return <section className="process section section-dark"><div className="container"><SectionIntro className="light" eyebrow="A considered approach" title={'From business problem<br />to <em>working software.</em>'}>We make the complex feel clear — and keep you involved at every important step.</SectionIntro><div className="process-grid">{[['01', 'Understand', 'We learn how your business currently operates, where work gets stuck and what success looks like.'], ['02', 'Design', 'We map your workflow and design the right system — no unnecessary features, no guesswork.'], ['03', 'Build', 'We develop and test the software around your requirements, with regular progress you can see.'], ['04', 'Launch & Improve', 'We deploy the system and continue improving it as your business and its needs grow.']].map(([number, title, text]) => <Reveal key={title}><article className="process-step"><span>{number}</span><h3>{title}</h3><p>{text}</p></article></Reveal>)}</div></div></section>; }
export function IndustriesSection() { return <section className="industries section"><div className="container industries-layout"><Reveal><div><p className="eyebrow">No one-size-fits-all</p><h2>Built for different kinds of <em>businesses.</em></h2><p className="section-lede">We don't assume every industry works the same. We listen first, then build around the way your business actually runs.</p><Link className="text-link dark-link" to="/contact">Don't see your industry? Tell us what you're trying to improve. <span>↗</span></Link></div></Reveal><Reveal className="reveal-delay"><div className="industry-cloud">{['Manufacturing', 'Distribution & Wholesale', 'Construction', 'Logistics', 'Retail', 'Professional Services', 'Other SMEs'].map((industry) => <span key={industry}>{industry}</span>)}</div></Reveal></div></section>; }
export function WorkflowDemo() { return <section className="workflow section section-tint"><Reveal><div className="container workflow-heading"><p className="eyebrow">A connected operation</p><h2>Imagine your entire operation <em>in one place.</em></h2><p>Start with the parts of the business that need it most. Connect the rest when you're ready.</p></div></Reveal><div className="container workflow-map">{['Customers', 'Orders', 'Inventory', 'Purchasing', 'Payments', 'Reports'].map((item, index) => <div className="workflow-node" key={item}><i>{['↗', '▤', '▦', '＋', '₹', '◒'][index]}</i><b>{item}</b><small>{['Relationships', 'Sales flow', 'Stock control', 'Suppliers', 'Cash flow', 'Clarity'][index]}</small></div>)}</div><p className="container workflow-caption">These modules can be connected into one system based on your workflow — so information moves with the work.</p></section>; }
export function WhyCustom() { return <section className="custom section"><div className="container custom-layout"><Reveal><div><p className="eyebrow">Built to fit</p><h2>Why build <em>custom?</em></h2><p className="section-lede">Off-the-shelf software makes you change your business to fit the product. Custom software gives your business room to work the way it should.</p></div></Reveal><Reveal className="reveal-delay"><div className="custom-points">{[['01', 'Your workflow', 'The software adapts to how your business operates — including the details that make it unique.'], ['02', 'Your data', 'Keep important business information organized and accessible in one system.'], ['03', 'Your team', 'Give employees exactly the tools they need without unnecessary complexity.'], ['04', 'Your growth', 'Start with what you need today and add functionality as the business grows.']].map(([number, title, text]) => <div key={title}><span>{number}</span><h3>{title}</h3><p>{text}</p></div>)}</div></Reveal></div></section>; }
export function CtaBand() { return <section className="cta-band"><div className="container cta-inner"><div><p className="eyebrow">Let's make work simpler</p><h2>Have a process that should be <em>simpler?</em></h2><p>Tell us what you're currently doing manually, and we'll help you figure out what can be turned into software.</p></div><Link className="button button-dark" to="/contact" onClick={() => trackEvent('primary_cta_click', { location: 'cta_band' })}>Book a Free Consultation <span>↗</span></Link></div></section>; }
